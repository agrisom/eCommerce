environment=DEV
url.path=http://localhost:8080

# Database
spring.data.mongodb.uri=mongodb+srv://${MONGOBD_USERNAME}:${MONGOBD_PASSWORD}@cluster0-akgrf.mongodb.net/test?retryWrites=true&w=majority
spring.data.mongodb.database=eCommerce_dev
#server.servlet.context-path=/api

# Token
jwt.secret=MySpringRestServiceDev
# 1 min
#jwt.time.ms=60000
# 10 Hour
#jwt.time.ms=36000000
# 5 days
jwt.time.ms=432000000

# Email config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true