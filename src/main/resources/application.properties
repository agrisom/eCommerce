environment=PROD
url.path=https://agrisom-shop.herokuapp.com/

# Database
spring.data.mongodb.uri=mongodb+srv://${MONGOBD_USERNAME}:${MONGOBD_PASSWORD}@cluster0-akgrf.mongodb.net/test?retryWrites=true&w=majority
spring.data.mongodb.database=eCommerce

# Token
jwt.secret=MySpringRestService
jwt.time.ms=36000000

# Email config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true